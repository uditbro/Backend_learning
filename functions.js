user = {
    username : "Chagan Mohan Tirpathi",
    price :299
}

function printUserDetail(anyobject){
    console.log(`user is ${anyobject.username} and price is ${anyobject.price}`);
}

printUserDetail(user)